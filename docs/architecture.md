# Architecture (archived)

Identity Studio and Colab launchers were removed. See [foocus_new](https://github.com/sunsideaspect/foocus_new) for the current Fooocus fork and Colab workflow.

Legacy API/worker layout (not maintained):

- `apps/api` — FastAPI jobs API
- `apps/web` — Next.js UI
- `workers/photo` — photo worker stub

Video worker was removed.
